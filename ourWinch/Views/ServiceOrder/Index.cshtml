@using ourWinch.Models.Dashboard;
@model IEnumerable<ServiceOrder>

/**
 * Represents a view displaying a list of service orders.
 * model IEnumerable<ServiceOrder> - The model representing a collection of service orders.
 */

<div class="container mt-5">
    <!-- The Bootstrap container class has been added.. -->
    <h2 class="text-center">Service Orders List</h2> <!-- The title has been centered. -->

    <div class="table-responsive">
        <!-- Wrapper for a responsive table. -->
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Fornavn</th>
                    <th>Etternavn</th>
                    <th>Mobil No</th>
                    <th>Email</th>
                    <th>Adresse</th>
                    <th>Feilbeskrivelse</th>
                    <th>Ordrenummer</th>
                    <th>Produkttype</th>
                    <th>Serienummer</th>
                    <th>Mottatt Dato</th>
                    <th>Årsmodell</th>
                    <th>Garanti</th>
                    <th>Servis</th>
                    <th>Reperasjon</th>
                    <th>Kommentar Fra Kunde</th>
                    <!-- If you want to add buttons for other actions, such as editing or deleting, you can also add those columns here. -->
                </tr>
            </thead>
                <tbody>
                    /**
                    * Iterates over each service order in the model and generates a table row for display.
                    */
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.ServiceOrderId</td>
                        <td>@item.Fornavn</td>
                        <td>@item.Etternavn</td>
                        <td>@item.MobilNo</td>
                        <td>@item.Email</td>
                        <td>@item.Adresse</td>
                        <td>@item.Feilbeskrivelse</td>
                        <td>@item.Ordrenummer</td>
                        <td>@item.Produkttype</td>
                        <td>@item.Serienummer</td>
                        <td>@item.MottattDato.ToShortDateString()</td> <!-- To show only the date, ToShortDateString() was used.. -->
                        <td>@item.Årsmodell</td>
                        <td>@(item.Garanti ? "Ja" : "Nei")</td>
                        <td>@(item.Servis ? "Ja" : "Nei")</td>
                        <td>@(item.Reperasjon ? "Ja" : "Nei")</td>
                        <td>@item.KommentarFraKunde</td>
                        <!-- If you want to add buttons for other operations, such as editing or deleting, you can also add those columns here.. -->
                    </tr>
                }
            </tbody>
        </table>
